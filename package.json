{
  "scripts": {
    "dev": "sass --watch --update src/sass:public/styles | live-server public --host=localhost --port=3000",
    "sass": "sass --watch --update src/sass:public/styles",
    "serve": "live-server public --host=localhost --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.54.9"
  }
}